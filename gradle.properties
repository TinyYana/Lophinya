group=fun.bm.lophine
mcVersion=26.2
apiVersion=26.2
channel=STABLE
clipVersion=1.0.1
weightVersion=1.0.0
# true for release, false for skip release, pre for pre-release
# 2026-08-07: set to "pre" for the candidate-C merge (DEC-72) so CI marks this as a
# pre-release instead of promoting it to "Latest" - this is an opt-in experimental
# performance feature (default off), not a recommended stable build yet. Flip back to
# "true" once candidate C has real production-scale validation.
release=pre
# true for push to repo, false for skip push repo, auto for detect by release value
pushRepo=auto

foliaRef=e48800d446d2bdeb24a8d31d671554440687e846

org.gradle.configuration-cache=true
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.vfs.watch=false
